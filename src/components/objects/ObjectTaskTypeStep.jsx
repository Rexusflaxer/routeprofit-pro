import React from "react";
import { ChoiceCard, StepHeading } from "./ObjectWizardUi";

export default function ObjectTaskTypeStep({ value, options, loading, error, onChoose }) {
  return <><StepHeading title="Wat betreft de taak?" description="Alleen categorieën met een toegevoegd beveiligingsplan zijn beschikbaar." />{loading ? <p className="rounded-xl border border-border/70 bg-muted/20 p-4 text-sm text-muted-foreground">Beveiligingsplannen laden...</p> : error ? <p className="rounded-xl border border-destructive/30 bg-destructive/10 p-4 text-sm text-destructive">{error.message}</p> : options.length ? <div className="grid grid-cols-1 gap-2 md:grid-cols-2">{options.map(option => <ChoiceCard key={option.value} selected={value === option.value} onClick={() => onChoose(option)} title={option.label} description={option.description} />)}</div> : <p className="rounded-xl border border-border/70 bg-muted/20 p-4 text-sm text-muted-foreground">Voeg eerst een plan toe in het tabblad Beveiligingsplan.</p>}</>;
}